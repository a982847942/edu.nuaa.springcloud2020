server:
  port: 80
spring:
  application:
    name: cloud-consumer-service

eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
ribbon:
  ReadTimeout: 10000
